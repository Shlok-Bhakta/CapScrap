<% content_for :title, "New item" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl text-content" data-font-size="36">New item</h1>

  <%= render "form", item: @item %>

  <%= link_to "Back to items", items_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium text-content", data: { font_size: 16 } %>
</div>
